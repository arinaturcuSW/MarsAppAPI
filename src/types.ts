export type PhotosParams = {
    roverName: string,
    cameraType?: string,
    paginationStart?: number,
    paginationEnd?: number,
    page?: number,
    sol?: number,
    earthDate?: string,
}
